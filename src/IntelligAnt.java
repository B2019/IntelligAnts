/**
 * A Simple class that runs the program.
 * @V 0.0.1
 * @Author 35769
 */
public class IntelligAnt {

    /**The main method to run from the command line (does not work with netbeans)
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        MainMenu run = new MainMenu();
    }
}
